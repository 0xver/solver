{
	"name": "@0xver/solver",
	"license": "AGPL-3.0-only",
	"version": "10.2.1",
	"description": "Basic building blocks for smart contract development",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/0xver/solver.git"
	},
	"devDependencies": {
		"prettier": "^2.3.1",
		"prettier-plugin-solidity": "^1.1.1"
	},
	"scripts": {
		"lint": "prettier --use-tabs --trailing-comma 'none' --plugin-search-dir . --check .",
		"format": "prettier --use-tabs --trailing-comma 'none' --plugin-search-dir . --write ."
	}
}
